# 当前进度

- ArchCritic 当前主线使用新版 React 前端 `frontend-v1-replica/` 和 FastAPI 后端 `backend/`；旧版 `frontend/` 和早期 `backend/prompts/` 占位记录已移出仓库，归档到 `../ArchCritic-开发过程历史代码/2026-06-13/`。
- 2026-06-13 已清理旧版代码残留：当前仓库不再保留旧版演示前端目录和早期 Prompt 占位目录；README、ARCHITECTURE 和前端迁移计划已同步更新。
- 2026-06-13 已准备 Git 版本提交：本轮包含新版前端流程打磨、后端草稿/资料接口更新、新增 Agent 与 logo 资源、旧代码归档删除和文档同步；提交前检查为前端类型检查 1/1、前端基础检查 18/18、后端测试 25/25 通过。
- 2026-06-13 已更新侧栏：左上角改为用户提供的 `archcritic-logo.png` 图片字标；版本菜单新增“批量编辑”，支持勾选多个历史版本后统一确认删除；删除确认按钮改为黑色“确认删除”，批量编辑和删除版本按钮已补独立图标；版本菜单图标与文字已统一对齐；批量删除时项目行不再整体 hover 变灰，仅删除按钮区域 hover 变灰；侧栏版本号只给已出评分的提交编号，草稿不再占用 V 编号；已按用户要求撤回“新建评图”按钮内部对齐改动，恢复原居中显示。
- 新建评图流程已进入细节打磨阶段：空白新建、继承已有项目、项目信息表单、任务书上传/编辑/删除、返回首页保存提醒都已接入。
- 本轮已继续修正继承与任务书交互：继承图标改为细线三节点分支符号，避免节点内重叠线；继承版本只按已完成 AI 评图的报告计为 V1/V2/V3；展开项目时标题行深灰、历史版本浅灰并倒序显示；点击整行即可继承。
- 本轮已修正草稿与任务书细节：修改任务书、图纸或 Agent 后返回首页都会触发未保存提示；保存并退出增加保存中状态；附件删除先弹确认卡；删除叉号悬停改为灰色；空设计说明不再显示“未填写设计说明”。
- 本轮已修复新建流程导航和确认页：继承版本返回会收起展开状态；新建方式弹窗点击遮罩可关闭；首页或侧栏打开草稿会回到上次停留步骤；流程下一步增加保存中状态；确认提交页改为真实项目信息、真实图纸、真实 Agent 和统一模型下拉。
- 本轮已修复空设计说明导致草稿保存失败的问题：后端允许草稿说明为空；前端保存失败会弹出原因卡片，不再静默停住；确认提交页继续优化了模型选择、项目信息排版、编辑按钮字号和 Agent 命名。
- 本轮已明确流程校验规则：返回首页可保存不完整草稿；点击下一步或确认提交时必须补齐当前步骤内容，并用卡片列出缺失项；确认页项目信息支持原地编辑和完成编辑保存，设计说明按三行高度显示并按需滚动。
- 本轮已继续修正确认提交页：统一“任务书”命名；步骤顺序恢复为“项目信息、选择阶段、上传图纸、确认提交”；模型选择字号固定为 14px；设计说明展示态和编辑态保持同一对齐、同一高度和同一滚动方式。
- 本轮已调整模型和 Agent 选择：确认页模型下拉改为向上弹出、灰色选中、无对勾；默认模型改为真实 `qwen3.6-plus`，旧演示模型草稿恢复时也转为真实默认模型；等待页模型名同步当前选择；阶段选择会自动带出对应 Agent，新增设计概念、图面表达、综合评审 Agent；确认页 Agent 最多 6 个可横向滚动显示。
- 本轮已继续微调 Agent 和上传页：新增的设计概念、图面表达、综合评审 Agent 已引用用户放入的三张图片；选择阶段页右侧固定显示全部 7 个 Agent，按当前阶段把对应卡片标紫；确认提交页 Agent 卡片尺寸回滚为原大卡片并用底部浅色横向滚动；模型下拉选中态改为深灰并与模型名左侧对齐；上传图纸页左侧列表加宽、右侧详情卡压窄并显示真实缩略图。
- 本轮继续收口上传图纸和确认页：阶段页改名为“选择设计阶段/启用的 Agent”；上传图纸页改为“批量编辑-勾选-确认删除”流程，普通状态不显示勾选框；右侧图纸详情补浅色滚动栏并改为“图纸说明”；确认页 Agent 横向滚动只在内容溢出时出现；模型下拉恢复浅灰选中并与模型名左侧对齐。
- 本轮继续修正模型下拉和图纸详情按钮：模型选项卡片恢复与模型按钮同宽并右侧对齐，选项文字左侧与当前模型名称左侧对齐；鼠标悬停其他模型时已选灰底暂时隐藏，移开后恢复；图纸详情“上一张”改为与“重新选择”一致的白底黑字按钮。
- 本轮继续微调模型和图纸详情：模型下拉改为窄卡片右贴齐，去掉左侧多余留白但保持选项文字对齐当前模型名称；图纸详情移除单张删除按钮，“重新选择/上一张/下一张”统一为白底黑字并放到图纸说明下方同一行。
- 本轮继续修正模型下拉对齐：确认是卡片 `p-1` 与选项左内边距叠加造成上方模型名偏右，已把选项左内边距下调，确保选项文字起点与下方当前模型名称起点对齐。
- 本轮继续完善上传图纸页：左侧选中图纸卡片增加颜色变化；“重新选择”改为替换当前图纸；长文件名在卡片内省略、详情区自动换行；图纸类型改为系统样式选择栏并支持二至七层平面图二次选择；右侧详情图可完整显示并支持点击放大查看。
- 本轮继续修正上传图纸详情卡片：移除详情区横向滚动，竖向滚动条更贴近卡片右边缘，详情内容保持居中；后端图纸类型白名单已补齐剖面图、立面图和二至七层平面图，选择后可同步更新详情名称和左侧图纸标注。
- 本轮继续完善上传图纸页：去掉详情卡片“已选中 1 张图纸”；图纸名称展示自动去掉 jpg/png/pdf 等后缀；上传时间改为后端真实创建时间；上传过程中左侧显示紫色“正在上传中...”临时卡片，完成后变为“上传成功”；普通上传完成后自动选中新图纸；图纸上传接口已支持 PDF 文件，前端可显示 PDF 卡片和详情预览。
- 本轮继续修正上传图纸页：详情上传时间按本机时区显示，解决 UTC 时间直显导致的小时差；批量编辑时未勾选图纸卡片变灰、勾选后恢复正常；PDF 图纸限制为单页上传，多页 PDF 会弹卡片提示，单页 PDF 在左侧卡片、右侧详情和大图预览中直接嵌入显示内容。
- 本轮继续修正 PDF 图纸显示方式：取消浏览器 PDF 查看器嵌入，新上传的单页 PDF 会在后端转换为 PNG 图纸预览并按普通图片显示；旧 PDF 记录不再嵌入查看器，而是提示重新上传生成图片预览。
- 本轮继续微调批量编辑：批量编辑时右侧详情选中的图纸卡片不再显示紫色选中叠加；若当前详情图纸被删除，详情会优先切换到它上一张未删除的图纸，而不是跳到第一张。
- 本轮继续修正重新选择反馈：替换图片或 PDF 时，对应旧图纸卡片会立即显示紫色“正在上传中...”状态，替换完成后再切换为新图纸并显示“上传成功”。
- 本轮继续修正上传等待态：重新选择替换时，左侧卡片和右侧详情会立即切换为新文件名；预览区显示空白占位“图片正在赶来的路上”，不再保留旧图。普通上传等待卡片也改为同一占位，不再显示默认图。
- 本轮继续修正保存草稿：顶部“保存草稿”会等待后端保存完成，并记录当前停留步骤；保存后再点返回首页不会重复弹未保存提醒。
- 本轮继续修正草稿恢复：图纸新增、替换、删除和任务书增删后会清掉当前提交详情缓存；从首页再次打开项目时会重新读取后端真实图纸，不再显示旧列表。
- 本轮检查通过：前端类型检查 1/1，前后端健康核查 1/1。

# 上次停在哪

- 侧栏 logo、版本批量编辑、菜单对齐、删除按钮 hover、删除确认卡片和版本编号规则已完成；“新建评图”按钮已恢复原居中显示，等待用户在 `http://127.0.0.1:4173/` 手动核对视觉和交互。
- 如果继续调整，优先处理用户指出的具体视觉或交互差异，不扩展新功能。

# 近期关键决定

- 新版前端视觉和按钮字号以 `DESIGN.md` 为准，同级按钮统一使用 `14px / 20px / 700`。
- 继承版本页中的“版本”只代表已完成 AI 评图并生成报告的提交；未完成草稿不单独计入 V 编号。
- 侧栏版本列表同样沿用“只有已出评分才编号”的规则；本地数据库中“城市社区活动中心”有 32 条提交，其中 18 条有分数、14 条无分数，无分数记录显示为“草稿”；旧草稿仍保留为独立记录，需用户手动批量删除，系统不自动合并或清理。
- 任务书、图纸和 Agent 修改都视为草稿变更，返回首页前必须提示保存或放弃。
- 草稿查看详情优先回到用户上次停留的流程步骤，不默认跳到确认提交页。
- 新建流程只有“进入下一步/确认提交”强制完整校验；“保存草稿/保存并退出”允许不完整内容。
- 当前模型选择会写入草稿并传给后端真实评图接口；当前后端方案阶段已有真实多 Agent 编排，概念/图纸阶段的完整多 Agent 后端编排仍需后续扩展。
- 后续涉及启动、更新后核查或热更新问题，继续使用 `.agents/skills/archcritic-dev-flow/` 固定流程。
