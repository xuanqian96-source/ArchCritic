# 当前进度

- 已在外层 `申请书和文本内容和前期资料/分工与工作安排` 补充团队用文档 `ArchCritic评图基准集制作说明_2026-05-22.md`，明确基准集选样、标注、互检和验收步骤。
- 已优化评图报告与历史版本：右侧评分横条去掉重复的综合评审项，左侧圆圈保留最终综合评审得分；“查看对比”改为“查看历史版本”，可打开历史版本卡片并切换回旧提交的表单、图纸和报告状态。验证结果：后端测试 22/22 通过，前端构建通过。
- 已完成报告问题与知识卡片引用链接、专项评分展开支持：反馈中的有效 `[K编号]` 可点击打开本次知识快照卡片，专项评分可展开查看分项分数、理由、证据、事实识别、信息缺口和不确定观察。验证结果：后端测试 22/22 通过，前端构建通过。
- 已整理方案阶段多 Agent 后续质量路线文档 `docs/plans/2026-05-22-agent-quality-validation-and-report-explainability.md`，明确真实图纸基准集、问题与知识引用、专项评分展开三条后续方向。
- 已优化方案阶段多 Agent 前端角色：评审矩阵和对话头像改为五个不同的像素小人，评审中会按角色显示阅读或写字动作，完成后的 Agent 卡片会变绿；底部历史版本追踪和评审矩阵都已补充横向滚动条。验证结果：前端构建通过。
- 已修复三项评图可信度问题：模型输入不再只读取第一张图纸，而是发送全部可用图纸并优先平面图；每次评图会保存当次知识库依据快照，历史报告不再重新检索后错位；功能 Agent 提示词与结构化输出 schema 已统一为 `observed_facts + sub_scores`。验证结果：后端测试 17/17 通过。
- 已按用户最新口径记录 ArchCritic 多 Agent 阶段划分：概念阶段为场地 Agent、几何形式 Agent、概念 Agent、综合评审 Agent；方案阶段为功能与流线 Agent、场地 Agent、几何形式 Agent、结构 Agent、综合评审 Agent；图纸阶段为图面表达 Agent 加方案阶段使用的 Agent，并由综合评审 Agent 汇总。
- 已修复百炼调用不稳定问题：百炼临时 OSS 图纸上传增加 3 次重试，找不到本地图纸时会明确报错；真实模型默认超时提高到 150 秒、输出上限提高到 2200 tokens；流式输出中断或 JSON 解析失败时，会自动改用非流式结构化评图兜底；前端现在能看到更具体的失败原因。已用 9.6MB、5009×6243 的原始 PNG 图纸实际复测千问 `qwen3.6-plus`：流式返回 127 段，最终完整生成报告，提交 #34 得分 78/B，知识库依据 6 条，百炼 URL 已保存。验证结果：后端测试 15/15 通过，前端构建通过；后端 `http://127.0.0.1:8000` 和前端 `http://127.0.0.1:5173/` 均已启动。
- 已继续修复评图可信度和知识卡片体验：流式评图结束时现在直接返回包含知识库依据的完整报告，不需要刷新页面才显示；功能 Agent 提示词新增 `observed_facts`，要求先识别楼梯、电梯、卫生间、主入口、车库入口、报告厅、服务台等事实，再输出评价；若模型“未看清”的内容与设计说明冲突，会自动从“必须修改”降级为“不确定观察”。知识库常见问题已拆成单张问题卡，卡片内容改为面向用户的精简版；案例图片现在通过 `/wiki-assets` 只读静态路径显示，知识卡片改为页面中央弹出。验证结果：后端测试 15/15 通过，前端构建通过；知识库图片访问正常。
- 已修复用户测试中发现的三类问题：知识库从固定读取改为按项目类型、阶段、图纸类型和设计说明检索相关卡片，并在提示词中要求引用编号；图纸上传改为默认保留原图，超过约 12MB 才高质量缩放到约 4000px 长边；提示词明确只评价已上传图纸范围，不把一层平面当整栋建筑完整提交。前端新增刷新恢复，会自动恢复上次项目、图纸、报告和历史记录。知识库常见问题补充了“一层平面误判整栋建筑”“车行入口与人行入口关系判断过度”“卫生间可见但标注不清”三条卡片。验证结果：后端测试 15/15 通过，前端构建通过；前后端服务仍运行正常。
- 已为前端增加模型选择栏，“开始 AI 评图”旁边可选择千问、Gemini 或演示模式；本轮默认测试切回千问 `qwen3.6-plus`。知识库追溯项已改为可点击链接，点击后会以知识卡片显示对应 Markdown 内容。后端评图接口已支持按单次请求传入模型来源和模型名，并在知识库依据中返回正文内容。验证结果：后端测试 15/15 通过，前端构建通过；后端已启动在 `http://127.0.0.1:8000`，前端已启动在 `http://127.0.0.1:5173/`。
- 已按知识库路线文档整理外层 `../知识库测试版`：新增 `01索引/评价维度索引.md`、`01索引/建筑类型索引.md`、`04常见问题`、`05评价维度`、`99维护记录` 和 `knowledge_index.json`；补齐 Mimesis Museum、New Acropolis Museum、Qinling Museum、Solomon R. Guggenheim Museum 4 个案例笔记。后端 `app/wiki.py` 已兼容读取新版 `05评价维度/{阶段}` 目录，本地 `.env` 已将 `WIKI_DIR` 指向 `../../知识库测试版`。验证结果：知识库 Markdown 共 33 个，内部链接缺失 0 个；方案阶段可读取 4 条依据，图纸阶段可读取 1 条依据；后端测试 15/15 通过。
- 已生成知识库技术路线文档 `docs/plans/2026-05-19-obsidian-wiki-knowledge-graph-route.md`：说明如何把 `../知识库测试版` 从 Obsidian 仓库逐步处理为可维护 Wiki、轻量索引、语义检索和后续知识图谱，并说明评图系统如何检索规范、案例和常见问题后交给大模型调用；已同步更新 README 和 ARCHITECTURE。
- 已核对成员提交的外层 `../知识库测试版`：该文件夹已包含 `.obsidian` 配置，可作为 Obsidian 仓库直接打开；共有 115 个文件，其中 Markdown 16 个、PDF/图片 94 个。首页、索引、规范笔记、案例笔记和原始资料结构完整；已检查 118 个 Obsidian 内部链接，缺失链接 0 个。原始优秀案例共 11 个，已整理成案例笔记 7 个，仍有 4 个案例只保留原始图片未写成笔记。
- 已查询并实测 Gemini 3.1 Pro：官方模型名为 `gemini-3.1-pro-preview`，Gemini 3 Pro Preview 已在 2026-03-09 停用并要求迁移到 3.1 Pro Preview；当前用户 API Key 直接调用 3.1 Pro 返回 429，错误提示免费层对 `gemini-3.1-pro` 的请求和输入 token 限额均为 0。结论：本项目技术上可改用 3.1 Pro，但需要开通计费；免费层继续使用 `gemini-2.5-flash` 更稳妥。
- 已检查用户填写的 Gemini 配置：`.env` 已读到 `LLM_PROVIDER=gemini`、`LLM_MODEL=gemini-2.5-flash`、Gemini Key 和官方 OpenAI 兼容地址；真实 Gemini 非流式调用成功返回评分、等级、摘要和 5 个分项，流式调用成功返回 13 段内容且 JSON 完整。因 Gemini 2.5 默认思考会占用输出额度并导致 JSON 截断，已为 Gemini 调用加入 `reasoning_effort="none"`；验证结果：后端测试 14/14 通过。
- 已为 ArchCritic 后端新增 Gemini 模型来源：`LLM_PROVIDER=gemini` 时复用 OpenAI 兼容客户端，读取 `GEMINI_API_KEY`、`GEMINI_BASE_URL` 和 `LLM_MODEL` 调用 Gemini API；已更新 `.env.example`、README 和架构说明。验证结果：后端测试 13/13 通过。
- 已修复前端流式对话体验：模型正文气泡改为收到第一段输出时再创建，保证流式输出显示在对话区底部；评图完成后不再自动跳转报告页，只提示用户点击“报告”查看。
- 已根据诊断结论完成代码修改：`dashscope` 模式会把本地图纸上传到百炼临时 OSS，缓存 `oss://` URL 和过期时间，模型调用时通过 URL 读取图纸，不再以 base64 直传作为主链路。
- 已新增流式评图接口 `/api/submissions/{submission_id}/evaluate-stream`，前端点击开始 AI 评图后会在右侧 AI 对话区实时显示模型输出，最终再渲染报告视图。
- 本轮验证结果：后端测试 12/12 通过，前端构建通过；真实百炼链路首次评图 127.4 秒返回，缓存临时 URL 后 36.0 秒返回；流式接口 37.9 秒完成并推送 119 段模型输出和 final 报告。
- 已完成百炼图像调用链路对照测试：同一张本地图纸 base64 直传多次 90 秒超时；按百炼官方临时 OSS URL 链路调用时，模型识别阶段约 13.1 秒返回，证明主要问题是当前 base64 图片传输方式，不是 `qwen3.6-plus` 不能识别建筑图纸。
- 已补充诊断脚本 `backend/scripts/diagnose_dashscope_vision.py`，用于后续复测纯文本、base64 图片、流式输出和 JSON 输出差异。
- 已将本地 `.env` 的百炼模型切回 `qwen3.6-plus`，确认当前配置读取到的模型名正确；密钥未输出。
- 已更新正式流程文档 `docs/plans/2026-05-15-qwen36-drawing-preprocess-evaluation-flow.md`，后续围绕 `qwen3.6-plus` 采用“原图保存、上传模型可访问 URL、图纸预处理、先读图摘要、再由功能 Agent 评分”的架构。
- 已调整第一个真实 Agent 产品文档 `docs/plans/2026-05-13-function-agent-product-design.md`：评分维度改为功能满足、功能分区、流线分析、平面丰富性，并补充 GPT 大模型、多模态识图能力和图纸阅读边界。
- 已开始构建第一个真实 Agent：新增功能与流线 Agent v1，能整理提交信息、上传图纸、Wiki 依据和设计说明，并通过 OpenAI 客户端返回前端报告结构。
- 已接入阿里云百炼 Model Studio：`LLM_PROVIDER=dashscope` 时复用功能与流线 Agent，走百炼 OpenAI 兼容接口，读取设计说明、图纸和 Wiki 依据后生成真实报告。
- 已解决百炼接入问题：原先 502 的直接原因是路由只给 `openai` 整理 Agent 上下文，`dashscope` 误走旧 payload；同时百炼 JSON 输出字段与预期不同，已增加 `scores/comments` 兼容解析。
- 百炼真实链路已通过：测试提交返回 200，总分 64，包含“功能与流线、功能满足、功能分区、流线分析、平面丰富性”五个前端展示项，并能指出缺少建筑平面图等具体建筑学问题。
- 已修复用户点击开始评图卡在 90% 的体验问题：前端上传前会压缩大图，右侧增加“报告/对话”切换，对话区显示连接后端、保存提交、上传图纸、调用模型和报告完成等状态；后端会跳过超过模型传输上限的大图并提示重新上传压缩图纸。
- 已为底部 Agent 评审矩阵增加人物头像样式和工作中呼吸动画，功能 Agent 调用时会显示工作状态。
- 已完成本轮核查：后端自动测试 10/10 通过，语法检查通过；OpenAI SDK 连接问题已定位为代理环境变量导致，并通过禁用 SDK 代理继承解决。
- 真实模型链路已跑到 OpenAI API，但当前账号返回额度不足或计费未启用，前端现在会收到清楚的中文错误提示。
- 已完成 0512 计划书第二步和第三步：点击“开始 AI 评图”后，前端会创建/复用当前项目、创建新提交、上传图纸、调用演示评图、动态更新右侧总分/分项/建议卡片、底部历史版本和多 Agent 状态，并从 `wiki-test/wiki` 读取知识库依据显示在“知识库追溯”区域。
- 后端新增 `app/wiki.py` 读取 Wiki Markdown 文件，新增提交知识库依据接口，新增项目历史版本接口；演示评图结果现包含 4 个 Agent 分项和 Wiki 引用依据。
- 本轮核查结果：后端自动测试 7/7 通过，前端构建通过，真实 HTTP 链路通过；真实链路覆盖健康检查、项目创建、连续两次提交、图片上传、演示评图、Wiki 依据、历史版本。
- 已确认用户再次无法连接的直接原因：前端 `localhost:5173` 正常运行，但后端 `localhost:8000` 当时没有启动。已启动后端服务，并按用户当前浏览器地址 `localhost:5173` 复测了健康检查、项目保存、提交保存、图纸上传、演示评图和报告查询，均通过。
- 已完成保存失败问题核查和修复：后端接口测试全部通过，真实 HTTP 链路复测通过；前端改为根据当前页面主机自动计算后端地址，并在保存时按真实步骤显示“连接后端、保存项目、保存提交、上传图纸”，后端不可达时会给出明确提示，不再只显示 `Failed to fetch`。
- 本轮复测覆盖：跨域预检、健康检查、项目创建、提交创建、图片上传、图纸列表、演示评图、报告查询、历史提交查询；结果均通过。自测生成的上传文件位于 `backend/uploads/`，已加入 `.gitignore`，避免误提交。
- 已定位用户点击“开始 AI 评图”后出现 `Failed to fetch` 的主要风险点：前端请求 `127.0.0.1:8000`，若后端未启动会连接失败；若旧 `.env` 只允许 `localhost:5173`，浏览器从 `127.0.0.1:5173` 访问时也会被跨域拦截。已在 `backend/app/main.py` 增加本地开发跨域兜底，同时允许 `localhost:5173` 和 `127.0.0.1:5173`。
- 已完成 0512 计划书第一阶段的代码开发：后端新增年级保存、图纸文件表、图片上传接口、图纸列表接口、单次提交查询和报告查询接口；前端点击“开始 AI 评图”时会创建项目、创建提交，并把已选择的图纸上传到后端保存。
- 已将后端上传目录配置为 `UPLOAD_DIR`，默认保存到 `backend/uploads`，并通过 `/uploads/...` 提供图片访问地址；旧版 SQLite 数据库启动时会自动补齐 `projects.grade` 字段。
- 已补充后端测试用例，但按照“数据库写操作需确认”的项目规则，本轮只运行了不写数据库的语法检查；前端构建检查已通过。
- 已生成 Demo 后端产品设计文档 `docs/plans/2026-05-12-demo-backend-product-design.md`，按当前前端工作台逐一区分了顶部项目信息、左侧提交、图纸上传、中间图纸区、右侧报告、知识库追溯、历史版本和多 Agent 矩阵需要的后端能力、接口和开发顺序。
- 已在 `frontend/index.html` 上完成第一批真实交互：左侧输入项目信息并点击“开始 AI 评图”后，顶部项目、类型、阶段会同步更新；图纸上传区会打开上传弹窗，可选择本地图片，并按总平面图、一层平面、分析图、效果图在中间图纸区查看。
- 已删除旧 React 验证页 `frontend/src`，前端改为纯静态 Vite 入口，避免两套前端代码并存。
- 已将 `../大创资料/ArchCritic-Demo/ArchCritic-前端演示Demo-（请使用电脑浏览器打开，仅用于展示初版前端设计，非最终成品）.html` 一比一复制为 `frontend/index.html`，当前本地前端入口可直接显示原 Demo 三栏评图工作台界面。
- 已补充两份协作与产品研究文档：成员工作指南用于指导知识库、前端和验收协作；过程问题研究用于负责人继续思考完成品标准、评图 Skill、推广盈利和市场调研。
- 已将正式开发阶段任务书改为新手友好的表格版，按负责人、前端 UI 成员、知识库成员分别列出任务、做法、工具、交付物和检查标准。
- 已完成 P0 的最小可运行闭环，并补写了实际完成情况总结文档。

# 上次停在哪

- 已完成组员可执行的评图基准集制作说明，并完成“知识引用”“专项评分展开”和“历史版本切换”；后续可继续做验证集或专项知识库质量提升。

# 近期关键决定

- 6 月前先完成研究型可演示原型，重点是上传、评图、结构化报告、历史记录、知识库依据和简化多 Agent 流程；暂不扩展完整账号权限和复杂向量数据库。
- 评图报告中的知识库依据以当次评图快照为准，避免后续知识库更新导致历史报告的 `[K1]`、`[K2]` 与卡片错位。
- 多 Agent 阶段划分采用最新版口径：概念阶段包含场地、几何形式、概念和综合评审；方案阶段包含功能与流线、场地、几何形式、结构和综合评审；图纸阶段在方案阶段 Agent 基础上增加图面表达 Agent，并由综合评审 Agent 汇总。
- 数据库先用 SQLite，同步方案比当前环境下的异步 SQLite 更稳定。
- 评图结果先用演示数据，先把完整流程跑通，再进入真实评图能力开发。
- 前端基础先以原 ArchCritic Demo 为准，保持界面展示一致，再做接口接入。
- 当前阶段已接入真实后端上传、提交持久化、右侧报告动态渲染、历史版本、刷新恢复和 Wiki 知识库检索追溯。
- 第一个真实 Agent 先选“功能与流线 Agent”，评分以功能满足、功能分区、流线分析、平面丰富性四项为准；图纸识别可接入多模态 GPT，但必须标注置信度，不能把不确定推测写成事实。
- 百炼主模型切回 `qwen3.6-plus`；正式方案不采用 384 像素低清图作为最终评图依据，也不再用 base64 直传作为主链路，而是通过模型可访问 URL、整体图、局部裁切图和图纸摘要提升速度与可靠性。
- 当前 `evaluate-demo` 接口会根据 `LLM_PROVIDER` 自动切换：`mock` 返回演示数据，`openai` 或 `dashscope` 调用真实功能与流线 Agent。
- 后端开发顺序定为：先保存项目与图纸，再动态渲染右侧报告，再接历史版本和知识库依据，最后升级真实单 Agent 与简化多 Agent。
- OpenAI 客户端改为按需导入，避免应用启动时卡住。
- Gemini 采用官方 OpenAI 兼容地址 `https://generativelanguage.googleapis.com/v1beta/openai/`，当前默认建议模型为 `gemini-2.5-flash`。
- Gemini 2.5 在本项目中需要关闭额外思考参数，否则短输出上限下可能返回被截断的 JSON。
- Gemini 3.1 Pro Preview 是当前更强的复杂任务和多模态推理模型，但 Free Tier 不可用；项目若切换到该模型，需要先开通 Google AI Studio/Cloud Billing。
- `../知识库测试版` 当前适合作为人读 Obsidian 仓库；若后续用于后端知识库检索，还需要再映射到 ArchCritic 现有 `WIKI_DIR` 和评价维度结构。
- 知识库落地路线采用“Obsidian Wiki 先行，轻量索引跟进，后续再升级知识图谱”的方式，避免一开始引入过重数据库工程。
- 当前先用 Markdown 轻量关键词检索作为可测试版本，`knowledge_index.json` 仍作为后续向量检索或知识图谱升级的初始结构说明。
- 本轮前端测试默认使用千问 `qwen3.6-plus`；模型选择先通过请求参数传给后端，不改全局密钥配置。
