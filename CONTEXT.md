# 当前进度

- 2026-07-16 本轮完整能力升级命名为 `v0.2.0 — 本地账户与三阶段真实评图`，准备同步至 GitHub；本地标注基准集不进入代码仓库，避免大文件和学生资料被公开上传。
- 2026-07-16 已完成本轮全栈完善：任务书正文真实进入模型并动态调整评分权重；概念、方案、图纸三阶段均按对应 Agent 真实评审；报告页移除虚构兜底数据；新增本地注册登录、账户数据隔离与历史恢复；后端上传文件改为登录后按所属账户读取；前后端超大文件已按职责拆分，业务代码均不超过 500 行。完整验证为后端测试 30/30、前端类型检查 1/1、基础检查 18/18、正式构建 1/1、前后端服务 2/2、热更新 1/1 通过。
- 2026-07-15 已完成揭榜挂帅后续系统开发计划、7 月 30 日会议论文大纲与两周补充建议、五人分工任务书和 18 页项目汇报 PPT 大纲；后续第一版聚焦公共建筑设计教学闭环，评分优化采用基准集、模型对比、教师区间、锚点与回归评测。
- ArchCritic 当前主线使用新版 React 前端 `frontend-v1-replica/` 和 FastAPI 后端 `backend/`；旧版 `frontend/` 和早期 `backend/prompts/` 占位记录已移出仓库，归档到 `../ArchCritic-开发过程历史代码/2026-06-13/`。
- 2026-06-13 已清理旧版代码残留：当前仓库不再保留旧版演示前端目录和早期 Prompt 占位目录；README、ARCHITECTURE 和前端迁移计划已同步更新。
- 2026-06-13 已准备 Git 版本提交：本轮包含新版前端流程打磨、后端草稿/资料接口更新、新增 Agent 与 logo 资源、旧代码归档删除和文档同步；提交前检查为前端类型检查 1/1、前端基础检查 18/18、后端测试 25/25 通过。
- 2026-06-13 已更新侧栏：左上角改为用户提供的 `archcritic-logo.png` 图片字标；版本菜单新增“批量编辑”，支持勾选多个历史版本后统一确认删除；删除确认按钮改为黑色“确认删除”，批量编辑和删除版本按钮已补独立图标；版本菜单图标与文字已统一对齐；批量删除时项目行不再整体 hover 变灰，仅删除按钮区域 hover 变灰；侧栏版本号只给已出评分的提交编号，草稿不再占用 V 编号；连续无分数草稿已归并为最后一次草稿显示；项目日期和排序已改为使用最后活动时间；已按用户要求撤回“新建评图”按钮内部对齐改动，恢复原居中显示。
- 新建评图流程已进入细节打磨阶段：空白新建、继承已有项目、项目信息表单、任务书上传/编辑/删除、返回首页保存提醒都已接入。
- 本轮已继续修正继承与任务书交互：继承图标改为细线三节点分支符号，避免节点内重叠线；继承版本只按已完成 AI 评图的报告计为 V1/V2/V3；展开项目时标题行深灰、历史版本浅灰并倒序显示；点击整行即可继承。
- 本轮已修正草稿与任务书细节：修改任务书、图纸或 Agent 后返回首页都会触发未保存提示；保存并退出增加保存中状态；附件删除先弹确认卡；删除叉号悬停改为灰色；空设计说明不再显示“未填写设计说明”。
- 本轮已修复新建流程导航和确认页：继承版本返回会收起展开状态；新建方式弹窗点击遮罩可关闭；首页或侧栏打开草稿会回到上次停留步骤；流程下一步增加保存中状态；确认提交页改为真实项目信息、真实图纸、真实 Agent 和统一模型下拉。
- 本轮已修复空设计说明导致草稿保存失败的问题：后端允许草稿说明为空；前端保存失败会弹出原因卡片，不再静默停住；确认提交页继续优化了模型选择、项目信息排版、编辑按钮字号和 Agent 命名。
- 本轮已明确流程校验规则：返回首页可保存不完整草稿；点击下一步或确认提交时必须补齐当前步骤内容，并用卡片列出缺失项；确认页项目信息支持原地编辑和完成编辑保存，设计说明按三行高度显示并按需滚动。
- 本轮已继续修正确认提交页：统一“任务书”命名；步骤顺序恢复为“项目信息、选择阶段、上传图纸、确认提交”；模型选择字号固定为 14px；设计说明展示态和编辑态保持同一对齐、同一高度和同一滚动方式。
- 本轮已调整模型和 Agent 选择：确认页模型下拉改为向上弹出、灰色选中、无对勾；默认模型改为真实 `qwen3.6-plus`，旧演示模型草稿恢复时也转为真实默认模型；等待页模型名同步当前选择；阶段选择会自动带出对应 Agent，新增设计概念、图面表达、综合评审 Agent；确认页 Agent 最多 6 个可横向滚动显示。
- 本轮已继续微调 Agent 和上传页：新增的设计概念、图面表达、综合评审 Agent 已引用用户放入的三张图片；选择阶段页右侧固定显示全部 7 个 Agent，按当前阶段把对应卡片标紫；确认提交页 Agent 卡片尺寸回滚为原大卡片并用底部浅色横向滚动；模型下拉选中态改为深灰并与模型名左侧对齐；上传图纸页左侧列表加宽、右侧详情卡压窄并显示真实缩略图。
- 本轮继续收口上传图纸和确认页：阶段页改名为“选择设计阶段/启用的 Agent”；上传图纸页改为“批量编辑-勾选-确认删除”流程，普通状态不显示勾选框；右侧图纸详情补浅色滚动栏并改为“图纸说明”；确认页 Agent 横向滚动只在内容溢出时出现；模型下拉恢复浅灰选中并与模型名左侧对齐。
- 本轮继续修正模型下拉和图纸详情按钮：模型选项卡片恢复与模型按钮同宽并右侧对齐，选项文字左侧与当前模型名称左侧对齐；鼠标悬停其他模型时已选灰底暂时隐藏，移开后恢复；图纸详情“上一张”改为与“重新选择”一致的白底黑字按钮。
- 本轮继续微调模型和图纸详情：模型下拉改为窄卡片右贴齐，去掉左侧多余留白但保持选项文字对齐当前模型名称；图纸详情移除单张删除按钮，“重新选择/上一张/下一张”统一为白底黑字并放到图纸说明下方同一行。
- 本轮继续修正模型下拉对齐：确认是卡片 `p-1` 与选项左内边距叠加造成上方模型名偏右，已把选项左内边距下调，确保选项文字起点与下方当前模型名称起点对齐。
- 本轮继续完善上传图纸页：左侧选中图纸卡片增加颜色变化；“重新选择”改为替换当前图纸；长文件名在卡片内省略、详情区自动换行；图纸类型改为系统样式选择栏并支持二至七层平面图二次选择；右侧详情图可完整显示并支持点击放大查看。
- 本轮继续修正上传图纸详情卡片：移除详情区横向滚动，竖向滚动条更贴近卡片右边缘，详情内容保持居中；后端图纸类型白名单已补齐剖面图、立面图和二至七层平面图，选择后可同步更新详情名称和左侧图纸标注。
- 本轮继续完善上传图纸页：去掉详情卡片“已选中 1 张图纸”；图纸名称展示自动去掉 jpg/png/pdf 等后缀；上传时间改为后端真实创建时间；上传过程中左侧显示紫色“正在上传中...”临时卡片，完成后变为“上传成功”；普通上传完成后自动选中新图纸；图纸上传接口已支持 PDF 文件，前端可显示 PDF 卡片和详情预览。
- 本轮继续修正上传图纸页：详情上传时间按本机时区显示，解决 UTC 时间直显导致的小时差；批量编辑时未勾选图纸卡片变灰、勾选后恢复正常；PDF 图纸限制为单页上传，多页 PDF 会弹卡片提示，单页 PDF 在左侧卡片、右侧详情和大图预览中直接嵌入显示内容。
- 本轮继续修正 PDF 图纸显示方式：取消浏览器 PDF 查看器嵌入，新上传的单页 PDF 会在后端转换为 PNG 图纸预览并按普通图片显示；旧 PDF 记录不再嵌入查看器，而是提示重新上传生成图片预览。
- 本轮继续微调批量编辑：批量编辑时右侧详情选中的图纸卡片不再显示紫色选中叠加；若当前详情图纸被删除，详情会优先切换到它上一张未删除的图纸，而不是跳到第一张。
- 本轮继续修正重新选择反馈：替换图片或 PDF 时，对应旧图纸卡片会立即显示紫色“正在上传中...”状态，替换完成后再切换为新图纸并显示“上传成功”。
- 本轮继续修正上传等待态：重新选择替换时，左侧卡片和右侧详情会立即切换为新文件名；预览区显示空白占位“图片正在赶来的路上”，不再保留旧图。普通上传等待卡片也改为同一占位，不再显示默认图。
- 本轮继续修正保存草稿：顶部“保存草稿”会等待后端保存完成，并记录当前停留步骤；保存后再点返回首页不会重复弹未保存提醒。
- 本轮继续修正草稿恢复：图纸新增、替换、删除和任务书增删后会清掉当前提交详情缓存；从首页再次打开项目时会重新读取后端真实图纸，不再显示旧列表。
- 本轮继续完善正在评图页：后端流式评图新增读取资料、知识库、模型输入和报告保存阶段事件；前端进度卡片按当前设计阶段和 Agent 列表生成，去掉大圆点、头像说明、追问输入框和模型显示，分析中卡片有紫色呼吸边框，完成变绿，出错变红；右侧改为浅灰原生滚动栏，进度卡片与下方日志框左右对齐；暂停评图会停留在当前页并切换为“继续评图”，暂停后忽略旧请求返回的残片或错误，报错后按钮改为“重新评图”，查看未完成报告改为遮罩提示；总体进度、已完成 Agent 和预计剩余时间改为根据真实状态估算。
- 本轮继续修正评图工作台：移除“检索专项知识库”前置进度卡片，保留各 Agent 自行检索知识库的逻辑；页面主标题固定为“评图工作台”；左侧 Agent 卡片补充说明文字，暂停或出错时显示“Agent 协同评图暂停”；总体进度改为只增不减，预计剩余时间改为按评图预算递减，避免评图中反向增加。已核查最近失败提交的 2 张图纸原文件存在，且都有千问可读取的模型地址；失败原因更可能是功能与流线 Agent 输出 JSON 不完整或被截断，现已提高该 Agent 输出预算，并增加 JSON 不完整时的精简重试。
- 本轮继续修正评图工作台细节：右下角文本输出框滚动条已改为与上方评图进度列表右边界对齐；确认热更新会刷新前端运行时状态，未完成的流式评图内容可能回到初始等待态，但已保存的提交、图纸和草稿不会丢失。
- 本轮继续调整报告页右侧对话：移除“重新生成”按钮；“继续与系统对话”说明改为可询问评分原因和不理解的问题；去掉设计稿写死的用户和系统示例，只保留系统开场白，未追问时显示普通字重的参考问题；系统头像改用用户放入的 `头像.png`；用户气泡改浅灰、系统气泡白底，双方气泡与头像间距统一；系统回答会在收到后以逐字方式显示，真实追问仍按当前提交保存并在再次打开该版本报告时恢复。
- 本轮继续优化报告页与评图工作台：报告页右侧去掉标题下方说明小字，追问推荐改为按当前报告生成的一行三张问题卡，点击后填入输入框，回车可发送；聊天区和输入框滚动条右侧对齐；聊天回答显示时会清理模型返回的 Markdown 加粗符号，后端追问提示词也要求不返回 Markdown；评图工作台两张大卡片标题和说明字号已统一到报告页对话卡片规格；知识库卡片图片改为通过后端完整地址加载，支持两列图片网格、点击放大和返回知识库卡片。
- 本轮继续微调报告追问和知识卡片：追问输入框文字改为黑色；模型回答里的 `**加粗**` 不再直接显示星号，而是渲染成真实粗体；后端追问提示词允许用加粗标注重点；知识库图片点击后改为全屏遮罩放大查看，点击图片外区域返回知识库卡片。
- 本轮继续修正反馈详情弹窗：反馈详情、知识库卡片和图片查看的遮罩改为覆盖完整浏览器窗口，不再露出两侧背景；发送按钮的等待省略号改为上下居中；知识库图片全屏查看支持鼠标滚轮缩放，最小尺寸保持初始完整显示大小，放大后可再缩小。
- 本轮继续修正反馈/知识库弹窗：弹窗卡片改为按浏览器窗口居中显示；图片查看去掉滚动容器，滚轮缩放灵敏度降低；图片放大后可按住鼠标左键拖动画面位置，缩回初始大小时自动回到居中。
- 本轮继续微调知识库卡片：知识库正文滚动区改为从标题下方延伸到卡片底部固定留白，减少底部过高空白，保持上下视觉留白一致。
- 本轮检查通过：前端类型检查 1/1，前端基础检查 18/18，后端测试 25/25，前后端服务检查 2/2。
- 2026-06-14 已启动本地前后端服务：后端 `http://127.0.0.1:8000` 和新版前端 `http://127.0.0.1:4173/` 均检查通过，服务检查 2/2。
- 2026-06-17 已重新启动本地前后端服务：后端 `http://127.0.0.1:8000` 和新版前端 `http://127.0.0.1:4173/` 均检查通过，服务检查 2/2。
- 2026-06-17 已完善历史版本对比页：版本折线改为固定间距并支持浅灰横向滚动，默认显示最新版本区间；纵轴改为 0-100 分数轴；圆点悬浮卡片改为移开即消失，并显示主要提升/下降、建议修改和方案优势。历史接口已补充必须修改和方案优势字段。验证为前端类型检查 1/1、前端构建 1/1、后端项目接口测试 2/2、服务和热更新核查通过。
- 2026-06-17 已继续细调历史版本对比页：默认进入时最新有分数版本贴近图表右侧，不再保留大段右侧空白；分数轴固定在左侧不随横向滚动移动；图表只显示已有评分的版本，未评分草稿不再占 x 轴位置；顶部得分变化改为最新版本对比上一个有分数版本，涨分绿色、降分红色。验证为前端类型检查 1/1、前端构建 1/1、服务和热更新核查通过。
- 2026-06-17 已继续优化历史版本对比细节：去掉分数轴“得分”标题，点位分数改为左对齐；下方版本名与左侧栏默认规则一致，默认显示 V 编号，用户改名后显示改名内容；点距略收紧并增加左侧安全空白，默认界面不遮挡最左侧版本名；右上角得分变化卡片改为一行纯文本，避免换行和溢出。验证为前端类型检查 1/1、前端构建 1/1、服务和热更新核查通过。
- 2026-06-17 已继续调整历史版本对比页：没有横向溢出时隐藏滚动栏，只有需要横向查看更多版本时才显示；项目只有一个有分数版本时不显示右上角相邻版本分数变化。已新增分数等级体系计划文档 `docs/plans/2026-06-17-score-level-system-design.md`，先定义等级名称、颜色、符号和升级/下降反馈，不写功能代码。验证为前端类型检查 1/1、前端构建 1/1、服务和热更新核查通过。
- 2026-06-17 已按新口径收窄分数等级体系计划：等级改为 60 分以下一档、60 分以上每 10 分一档，共五档；等级只显示在历史版本对比页右上角分数变化处；文档补充了专业/过程/有趣/建筑隐喻四组等级名称候选、每档 5 条短评语料，以及“建筑设计稿成长图标”的详细设计说明。
- 2026-06-17 已重新调整分数等级体系计划：核心从“设计稿成长图标”改为参考多邻国式的分数游戏化激励，采用青铜、白银、黄金、铂金、钻石五档奖牌段位；文档补充了段位名称方案、每档 5 条提分激励短评、奖牌图标规格、晋级/同段涨分/降级反馈动效。
- 2026-06-17 已继续完善分数等级体系计划：确认使用标准游戏段位 A 方案，不做段内小级别；重写短评语料为更口语、更有人味的鼓励和轻调侃；补充掉段语料库、掉段显示格式、掉段判定和奖牌下滑淡出动效。
- 2026-06-17 已先实现历史版本对比页分数等级静态效果：右上角分数变化旁显示当前等级奖牌和名称；折线点按青铜、白银、黄金、铂金、钻石五档换色；点位悬浮卡片按对应等级换背景色并显示等级名称。本轮暂不实现切换动效和掉段动画。
- 2026-06-17 已继续微调历史版本对比页等级效果：右上角当前等级改为直接图标加文字，不再做胶囊卡片；去掉鼠标靠近点位时的竖向辅助线；具体维度评分的 V 编号改为沿用综合评分历史版本编号；“总得分变化”按钮改名为“综合评审得分”；折线从紫色改为深灰。设计文档已补充新增内容默认不做卡片嵌套的要求。
- 2026-06-17 已继续收敛历史版本对比页视觉：右上角去掉“当前等级”、等级名和奖牌，只保留版本范围与分数变化；分数变化字号对齐上方指标按钮；左侧分数轴字号和下方版本名统一，并改为左对齐；折线改为更轻的灰色；等级点和悬浮卡片颜色收敛到主紫色相近色系，悬浮卡片标题不再显示等级名和图标。
- 2026-06-17 已继续修正字体和报告页等级色：历史分数轴显式使用与下方版本名相同的字体、字号、字重和颜色；报告页评分维度大卡片、综合分圆环和圆环下方等级标签改为按“不及格、及格、良好、优秀、卓越”五档换色。
- 2026-06-17 已继续打磨报告页和历史入口：评分维度选中标题与下方大卡片同色，卡片正文改为分段且五行区域可滚动；历史版本入口会后台预取已评分版本数量，不足两份时弹出“暂时没有历史版本对比”提示；查看项目信息改为报告页内居中弹窗，图纸可点击放大；报告页和历史页侧栏主按钮改为“返回首页”；历史点位悬浮卡片可点击进入对应版本报告。验证为前端类型检查 1/1、前端构建 1/1、服务和热更新核查通过。
- 2026-06-17 已微调历史版本不足提示卡片：按“草稿尚未保存”大提醒卡片规格统一为 592×238、标题 24px、正文 16px、28px 内边距，按钮固定右下角并保持黑底白字。验证为前端类型检查 1/1、服务和热更新核查通过。
- 2026-06-17 已优化历史版本对比加载体验：同名项目的完整历史记录会在报告页后台提前拉取并缓存，进入历史页时优先同步读取缓存，后台刷新只做补齐，减少版本多时先空白或先显示默认数据再跳变的问题。验证为前端类型检查 1/1、前端构建 1/1、服务和热更新核查通过。
- 2026-06-17 已继续修正历史点位和报告弹窗：历史点位悬浮卡片恢复为离开圆点即消失，点击圆点本身会进入对应版本报告；报告页下钻到 25 分制子维度时，颜色按每 5 分一档换算五档等级；反馈详情、知识库卡片和返回反馈详情的紫色标题文字统一为项目信息同款 13px。验证为前端类型检查 1/1、前端构建 1/1、服务和热更新核查通过。
- 2026-06-17 已修复历史入口误弹和点位切换等待：报告页点击历史版本对比时会先等待同名项目完整历史缓存返回，再判断是否不足两份，避免刚打开 V14 等单项目历史时误弹“暂无历史版本”；历史页会分批后台预取各评分版本报告，鼠标移入点位时优先预取该版本，点击圆点时复用缓存以减少进入报告的等待。验证为前端类型检查 1/1、前端构建 1/1、服务和热更新核查通过。
- 2026-06-18 已启动本地前后端服务：后端 `http://127.0.0.1:8000` 和新版前端 `http://127.0.0.1:4173/` 均检查通过，服务检查 2/2。
- 2026-06-18 已完成两处弹窗交互小调整：反馈详情右上角关闭叉号已去掉，保留点击遮罩关闭；项目信息弹窗和上传图纸页的大图查看已复用知识库图片的滚轮缩放、按住左键拖动和默认最小尺寸逻辑。验证为前端类型检查 1/1、服务和热更新核查通过。
- 2026-06-18 已微调项目信息弹窗的评图设置：Agent 不再显示为紫色标签，改为“调用 Agent：Agent 名称”的普通文字行，并与模型名称保持同一字体样式。验证为前端类型检查 1/1、服务检查 2/2、报告页热更新核查通过。
- 2026-06-18 已优化新建评图流程体验：项目信息弹窗去掉未收集的“课程名称”；前几步切换改为校验通过后先进入下一页、后台非阻塞保存，未修改时不再提交保存；草稿保存增加队列合并保护，避免连续切页重复创建；固定 Agent 图片提前预加载；流程页、按钮、表单和下拉增加克制的 fade/slide 轻量动效，并兼容减少动态效果设置。验证为前端类型检查 1/1、前端构建 1/1、前端基础检查 18/18、服务和热更新核查通过。
- 2026-06-18 已微调报告页评分维度卡片：摘要文本未超过五行时不显示滚动条，只有真实超过五行后才出现滚动条。验证为前端类型检查 1/1、服务检查 2/2、报告页热更新核查通过。
- 2026-06-18 已修正评分维度卡片五行仍出现滚动条的问题：原因是摘要被拆为两段时段落间距额外增加 4px，虽然视觉上是五行，但实际高度超过原 100px 容器；现已按五行文字加段落间距计算阈值，五行内不显示滚动条。验证为前端类型检查 1/1、服务检查 2/2、报告页热更新核查通过。
- 2026-06-18 已修复左侧栏版本号与报告标题版本号不一致的问题：原因是报告标题旧逻辑按同名项目记录位置编号，`projectId=19` 被算成 V11，而左侧栏按同名项目下所有已评分提交编号，提交 35/36 应为 V14/V15；现报告标题复用左侧栏同一套版本编号。左侧栏选中版本行也已补灰底和文字加粗效果。验证为前端类型检查 1/1、前端构建 1/1、前端基础检查 18/18、服务检查 2/2、报告页和侧栏热更新核查通过。
- 2026-06-18 已修复新项目上传图纸页仍显示设计稿占位符的问题：原因是前端无图纸时仍使用写死的示例图纸兜底；现已移除这些假图纸、假缩略图和假上传数量，左侧列表与右侧详情在无图纸时只显示灰色空状态提示。验证为前端类型检查 1/1、服务检查 2/2、上传页热更新核查通过。
- 2026-06-18 已继续微调上传图纸页右侧详情：无图纸时不再放入滚动容器，避免空详情卡片显示滚动栏；空状态文案改为“上传后这里可以查看图纸详情。”。验证为前端类型检查 1/1、服务检查 2/2、上传页热更新核查通过。
- 2026-06-18 已对齐上传图纸页两张卡片的空状态文字：右侧详情空状态改为和左侧上传列表同一内容区高度居中，避免因左侧顶部按钮造成两边文字高度不一致。验证为前端类型检查 1/1、服务检查 2/2、上传页热更新核查通过。
- 2026-06-18 已修复上传图纸页图纸说明输入异常：原因是说明框每输入一个字就请求后端并刷新图纸对象，同时全局 textarea 聚焦样式生成了额外内框；现改为本地即时输入、离开输入框后保存，并去掉内框，字体大小和颜色与图纸类型文本一致。验证为前端类型检查 1/1、服务检查 2/2、上传页热更新核查通过。
- 2026-06-18 已补齐图纸说明进入模型输入：后端功能 Agent 和方案阶段专项 Agent 的“已上传图纸”上下文会包含每张图纸的说明；前端上传页进入下一步或保存草稿前会先保存当前图纸说明。右侧详情图点击放大改为更稳定的指针事件触发，避免被说明框失焦保存或滚动容器打断。验证为前端类型检查 1/1、后端相关测试 7/7、服务检查 2/2、前端热更新 3/3 通过。
- 2026-06-18 已继续修复上传与确认页回归问题：右侧详情图放大增加点击兜底触发；确认提交页项目信息编辑输入框去掉紫色聚焦内框，完成编辑改为立即退出并后台保存；已上传图纸列表改为横向滚动，超过 6 张时不再挤到下一行，滚动栏位于卡片底部。验证为前端类型检查 1/1、后端相关测试 7/7、服务检查 2/2、前端热更新 4/4 通过。
- 2026-06-18 已最终加固上传页详情图放大：原因是前次使用 pointerdown/pointerup 打开遮罩，可能被同一次点击的后续事件立即关闭；现改为只在 click 完成后打开，并通过 Portal 把预览遮罩挂到页面 body，预览图片地址、名称和类型也固化到独立对象，避免被图纸说明保存或页面动画层影响。确认页已上传图纸区域去掉右侧编辑按钮，卡片上移、图片显示区增高，底部滚动条不再遮挡卡片。验证为前端类型检查 1/1、前端构建 1/1、服务检查 2/2、前端热更新 3/3 通过。
- 2026-06-18 已优化评图工作台进度体验：总体进度改为按每个评图节点的预计耗时伪实时增长，节点超时未完成时停在该节点尾部等待真实完成；右侧任务卡片完成后会在被遮挡时自动滚到完整可见，下方流式日志有新内容时自动滚到底。验证为前端类型检查 1/1、服务检查 2/2、前端热更新 3/3 通过。
- 2026-06-18 已修正知识库卡片标题换行后的间距：知识库正文区域不再使用固定顶部位置，改为跟随标题高度自然下移，确保两行标题时 K 编号与标题的间距和单行标题一致。验证为前端类型检查 1/1、服务检查 2/2、前端热更新 1/1 通过。
- 2026-06-18 已调整首页最近评图项目卡片颜色：有分数的已完成项目不再使用绿色，改为复用历史版本分数段的五档紫色体系，分数、状态标签和圆点颜色随分数段变化。验证为前端类型检查 1/1、服务检查 2/2、前端热更新 2/2 通过。
- 2026-06-18 已修复三处真实测试问题：全局 `input/textarea` 聚焦样式不再给文本输入框加紫色方框；空白新建评图时若项目名与已有项目重名，会在下一步和保存草稿前提示更换名称，避免报告被归到旧项目组；评图工作台进度改为按当前节点激活时间独立推进，显示一位小数，减少一段一段跳动。验证为前端类型检查 1/1、服务检查 2/2、前端热更新 3/3 通过。
- 2026-06-18 已按要求调整评图工作台进度显示：内部仍保留小步进更新，但界面百分比只显示整数。验证为前端类型检查 1/1、服务检查 2/2、前端热更新 1/1 通过。
- 2026-06-19 已启动本地前后端服务：后端 `http://127.0.0.1:8000` 和新版前端 `http://127.0.0.1:4173/` 均检查通过，服务检查 2/2。
- 2026-06-19 已接入新增静态官网首页：React 入口直接嵌入 `homepage-cn/archcritic-homepage-cn-figma-dark.html`，旧登录页不再作为入口；首页“体验 Demo”进入系统项目卡片页，系统内点击左上角 ArchCritic logo 返回新官网首页。验证为前端类型检查 1/1、前端构建 1/1、服务检查 2/2、前端热更新 1/1、静态首页访问 1/1 通过。
- 2026-06-21 已启动本地前后端服务：后端 `http://127.0.0.1:8000` 和新版前端 `http://127.0.0.1:4173/` 均检查通过，服务检查 2/2。

# 上次停在哪

- 本轮功能和代码重构已经完成，停在最终本地服务核查；下一阶段应按 2026-07-15 开发计划，用真实任务书和学生图纸做三阶段回归验收，并建立教师评分对照。
- 当前任务书支持带文字层的 PDF、DOCX、TXT；扫描版 PDF 尚未接入 OCR，会明确要求用户改传可读取版本。

# 近期关键决定

- 参赛版按任务书中更早的 9 月 5 日提交口径倒排，8 月 31 日内部冻结；7 月 30 日前只优先补论文所需教学案例、试用数据和研发基线。
- 新版前端视觉和按钮字号以 `DESIGN.md` 为准，同级按钮统一使用 `14px / 20px / 700`。
- 继承版本页中的“版本”只代表已完成 AI 评图并生成报告的提交；未完成草稿不单独计入 V 编号。
- 侧栏版本列表同样沿用“只有已出评分才编号”的规则；多个无分数草稿在侧栏归并为最后一次草稿显示，旧草稿记录不自动删除；归并后只有一个草稿或一个版本时不显示下拉箭头，直接点击项目进入。
- 任务书、图纸和 Agent 修改都视为草稿变更，返回首页前必须提示保存或放弃。
- 草稿查看详情优先回到用户上次停留的流程步骤，不默认跳到确认提交页。
- 新建流程只有“进入下一步/确认提交”强制完整校验；“保存草稿/保存并退出”允许不完整内容。
- 当前模型选择会写入草稿并传给后端真实评图接口；概念、方案、图纸三阶段均使用阶段白名单执行对应专项 Agent，报告保存当次任务书摘要、权重和启用 Agent，保证历史结果可追溯。
- 当前账户与数据只保存在本机 SQLite 和上传目录中；密码使用不可逆哈希，会话使用 HttpOnly Cookie。首个正式注册账户会接管升级前的本地历史项目，后续账户严格隔离。
- 后续涉及启动、更新后核查或热更新问题，继续使用 `.agents/skills/archcritic-dev-flow/` 固定流程。
