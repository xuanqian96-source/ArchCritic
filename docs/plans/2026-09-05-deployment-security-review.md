# 2026-09-05 题库、内测注册与部署审查

用户已批准数据库备份、补建用量表、服务器上传、前后端发布及 npm 元数据漏洞扫描；指定一个内测码，其余两个随机生成，每码 100 人。明文码只在私密交付中保存，不进入仓库。生产前端依赖扫描结果：已知漏洞 0 项；此结论不覆盖开发依赖、Python 依赖或业务逻辑。

## 发布实况

- **用户最终确认百度后台已有访问记录，随后确认安装检测显示“安装正确”**，网站访问统计与安装检测均完成验收。官方原始片段提交 `0593a3e` 已部署；浏览器核验加载一次，采集请求的站点 ID 正确、返回 200，页面错误 0。后台结果由用户确认，不声称助手登录了其百度账户。

- 用户随后在 HTTPS 检测仍得到未检测提示，因此 HTTP 不是充分解释。模拟百度抓取标识访问首页返回 200，包含正确 ID 与最新脚本，未发现拦截或旧缓存；此前加载片段添加了业务条件，现改成与用户提供的官方原文完全一致，自动 PV 与路由条件单独配置。此改动是恢复官方安装格式，不宣称已证明它是检测误报的根因。实际采集与后台安装检测继续分别报告。

- **百度统计最终验证成功**：前端提交 `4f86e66` 已上线，正式首页源码包含头部统计代码；浏览器检查头部标记 1 个、外部脚本加载 1 次，`hm.js` 返回 200 / 29,899 字节，`hm.gif` 返回 200 / 43 字节，页面错误 0。此前空响应现已消失，已有实际采集成功证据；不将网站采集验证等同于已登录百度后台核对报表。

- 用户改正检查地址后，百度显示“未安装代码”。依据官方说明，检查器可能无法识别 JS 动态安装；现已改为前端公开配置 + 构建时头部安装，首页原始 HTML 包含完整官方加载地址，应用只复用统计实例发送路由 PV。前端基础 130/130、类型与构建 2/2、安装去重及环境限制 4/4 通过，正式发布后还需区分源码检查与实际采集结果。

- 后续用户提供百度“无法正常访问”截图，目标是 `http://www.archcritic.cn`。国内服务器独立验证 `www.archcritic.cn` 无法解析，`https://archcritic.cn` 返回 200。检测应使用正式根域名 HTTPS 地址；这解释了检测器的访问失败，但尚不能解释统计官方脚本返回空内容。

- 功能提交 `7b8b562` 已推送 GitHub；服务器已切换到 `/opt/archcritic/releases/20260905-7b8b562`，EdgeOne 正式页面已加载 `app-Ib6nhCCK.js`，注册页内测码栏可见。
- 数据库与原配置已备份到 `/var/backups/archcritic/20260905-7b8b562`，切换前再次停服快照，原用户记录和 58 个项目逐条保持不变；注册验证在备份副本进行，正式库没有新增测试账户、内测码累计使用仍为 0。
- **注册账号实际为 5 个，不是 53 个。** `users` 表另有 48 条 4—6 月历史项目拥有者记录，账号和密码字段均为空。旧代码（`01de962` 的父版本）在每次创建项目时执行 `User(name=payload.owner_name, role="student")`，早于 7 月 16 日登录注册功能。它们不能登录，也不是本次新增用户；此前将 53 条表记录称为 53 个账户的说法不准确。
- 服务器备份副本验证新版题库共 94 道（入门 26、进阶 41、研习 27），注册及答案提交成功。公网已确认新版答案路由、未登录访问题库返回 401、无码注册返回 403、统计 ID 配置正确。
- 正式浏览器检查注册页正常、脚本错误为 0。百度统计脚本虽然返回 200，但响应内容为 0 字节，统计队列仍是普通数组，没有采集请求；从腾讯服务器独立请求及绕缓存请求得到相同结果。用户已粘贴完整代码，逐字确认 ID 与配置一致。不能据此声称统计已生效，等待百度后台状态核实及官方脚本恢复实际内容。

## 已证实的题库原因

- 正式服务实际工作目录为 `/opt/archcritic/current/backend`，软链接目标为 `releases/20260902-89b5a7c`；服务启动时间仍为 9 月 2 日。
- 线上接口清单只有 `/api/knowledge/quiz`，没有 `/api/knowledge/quiz/answer`；`app.services.knowledge_quiz` 模块不存在。
- 服务器 `/var/lib/archcritic/wiki` 的知识卡和答案章节存在，旧接口构造器能够读取 100 条旧格式题目。因此是后端代码未同步，不能归因于题库文件未上传。
- 新前端需要包含题型、选项及结构化难度的新接口。既有 9 月 3 日记录仅检查接口存在，未检查接口格式，不能作为新版部署成功证据。本次以服务器实际代码与接口结构纠正该记录。

## 安全审查结果

| 优先级 | 发现与证据 | 当前状态 |
| --- | --- | --- |
| 高 | 评图和两个助手入口有登录保护，但未发现账户调用额度或服务级并发上限；同一用户可反复启动昂贵请求，评图并发还可能争用同一报告。 | 未更改算力策略；内测码只能限制账户数量。 |
| 高 | `/wiki-assets` 在 `app/main.py` 挂载整个知识库；线上匿名请求原始 Markdown、`.archcritic` JSON 均返回 200，绕过浏览接口的隐藏章节及登录限制。 | 已确认，不把原文/内部记录内容复制进报告；后续应限定公开媒体范围。 |
| 中 | 注册、登录、账号可用性接口没有请求频率限制；密码哈希运算也在异步处理函数内同步执行。 | 本次新增注册内测码门槛；不等于登录防刷。 |
| 中 | 图纸上传先 `await file.read()`，再检查 30MB；`pdftoppm` 转换没有执行超时。 | 大请求或复杂文件可能消耗内存、CPU；未执行压力或恶意文件测试。 |
| 中 | 生产 Cookie 为 `Secure/HttpOnly/SameSite=None`；没有服务器端 Origin/CSRF 校验。退出接口无请求体要求，可被跨站表单触发；不能把 CORS 视为防 CSRF。 | 未修改跨域会话策略，避免破坏现有登录。 |
| 中 | 修改密码只更新密码哈希，未撤销既有会话。 | 已泄漏会话可能继续使用到过期；未操作线上会话。 |
| 低 | 前端 HTTPS 有 HSTS，但没有 CSP、X-Frame-Options、nosniff；API 有 nosniff，没有 HSTS。 | 属于防御配置缺口，不单独认定为已可利用漏洞。 |
| 低 | 首账户历史接管原来更新全部项目。 | 本次收紧到无归属项目；不修改原有项目数据。 |

已看到的保护：密码使用带随机盐的 PBKDF2；会话令牌只在数据库保存哈希；用户图纸、项目和提交接口做归属检查；上传路径做目录边界检查；生产后端只监听 `127.0.0.1:8000`；服务使用独立用户及 NoNewPrivileges；正式 HTTPS 当前可用。这些不构成完整渗透测试或“没有漏洞”的结论。

22、22222、80、3000、443 处于监听状态，80/3000 属于同机其他服务，本轮未扫描或修改其他服务；主机监听不等于已证明全部端口公网可达。

## 已完成的代码

- 注册页增加内测码；服务端采用摘要白名单和每码累计限额，默认开启门槛，无码配置时暂停新注册。
- 新增 `registration_code_usage` 表，额度与用户创建一同提交；并发扣减、失败回滚；既有用户不需要内测码。
- 提供生成多个随机码的离线脚本，默认三个，每码 100 人（合计 300 人）。不向前端发送内测码或摘要。
- 百度统计由后端提供公开站点 ID，前端异步加载官方脚本并按路由统计；未提供站点 ID 前关闭统计。
- 题库前端拒绝旧接口结构并清除旧版本缓存，显示明确更新提示。

## 发布步骤及状态

1. 已从用户截图取得百度统计站点 ID `4a3c0c6e9d7a08255ca1ac7e46b4895e` 并应用到正式配置。
2. 已获授权并完成生产数据库备份与新增用量表；不迁移、覆盖或删除既有用户和项目。
3. 已上传代码、更新服务器私密配置并切换后端；GitHub 推送和 EdgeOne 页面更新均完成。
4. 已验证题型字段、答案接口和静态构建版本；统计安装已改为静态头部代码，文字版代码核对一致，有效脚本和实际采集请求均验证成功。用户可刷新百度安装检查及报告查看处理后的数据。

初次自动审批曾拒绝候选代码上传及 `npm audit` 元数据外发；随后用户明确授权，两项操作已正常完成，没有绕过拒绝。npm 生产依赖检查结果为 0 个已知漏洞。

## 验证

- 后端专项：8 通过、0 失败（认证、内测额度、失败回滚、并发及题库）。
- 前端基础：130 通过、0 失败；类型检查及正式构建：2 通过、0 失败。
- 本地服务：2 通过、0 失败。
- 后端其余完整回归达到 60 秒上限中止，未完成，不计为通过；未执行真实模型评图或压力测试。
