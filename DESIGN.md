# ArchCritic 设计规范

本文档用于约束 ArchCritic 新版前端的视觉系统、文字层级、组件样式和页面布局。后续修改 Figma 或 React 前端时，应优先遵守本文档；如果局部页面为了像素级还原 Figma 需要例外，应在代码或后续记录中注明原因。

## 1. 设计目标

ArchCritic 是面向建筑设计评图的专业工具，整体视觉应保持克制、清晰、可信。界面不追求装饰感，而是让用户能快速理解：当前项目是什么、评图到哪一步、系统给出的判断和下一步操作是什么。

核心原则：

- 信息层级靠字号、字重、行高、间距共同建立，不只依赖颜色。
- 同一层级文字在不同页面保持一致，例如页面标题、卡片标题、正文、说明文字、按钮文字不得随意变化。
- 大面积使用浅灰背景和白色卡片，紫色只用于关键操作、选中状态和系统强调。
- 所有页面以固定 `1536 × 820` 设计画布为基准，浏览器中等比缩放并水平居中。
- 组件优先复用统一样式，不为单个页面临时创造新的圆角、阴影、字号。

## 2. 设计来源

本规范基于以下内容整理：

- Figma 文件“前端 V1”中的组件基准页、官网首页、登录页、项目首页、项目卡片、侧栏、弹窗和报告页。
- 当前实现目录 `frontend-v1-replica/` 中的 React + TypeScript + Tailwind CSS 前端。
- 当前产品状态：项目首页、项目创建流程、图纸上传、等待评图、报告页、历史版本、账号资料和侧栏项目版本管理。

## 3. 基础画布与布局

### 3.1 画布

- 基准尺寸：`1536 × 820`
- 页面背景：`#f4f6f8`
- 画布缩放：保持内部坐标不变，按浏览器尺寸等比缩放。
- 页面居中：缩放后的整体画布应水平居中，左右留白一致。

### 3.2 工作区布局

主要工作台页面使用三段式布局：

- 左侧系统栏：`248 × 796`，位置约 `x=23/24, y=11/12`
- 主内容区起点：`x=307/308`
- 页面主标题：`x=307/308, y=53/54`
- 页面副标题：`x=309/310, y=107/108`
- 首页主项目卡片区域：`803 × 622/624`，位置约 `x=307/308, y=182/183`
- 首页右侧信息卡片：宽 `324`，位置约 `x=1146/1147`

布局规则：

- 左侧栏是所有工作台页面的对齐基准。
- 主内容区所有卡片左边缘应与页面标题左边缘保持一致。
- 同一页面内卡片之间优先使用 `24px` 间距。
- 不允许因为内容不足而让页面整体偏左或偏右。

## 4. 字体系统

### 4.1 字体族

推荐字体顺序：

```css
font-family: "Inter", "Noto Sans SC", "Microsoft YaHei", sans-serif;
```

使用说明：

- 英文、数字、分数、版本号优先使用 `Inter`，保证界面接近 Figma 和 ChatGPT 风格。
- 中文使用 `Noto Sans SC` 回退，保证中文清晰、稳定。
- 不使用衬线字体。
- 不使用浏览器默认字体。

### 4.2 Typography Scale

| 层级 | 用途 | 字号 | 行高 | 字重 | 字间距/说明 |
|---|---|---:|---:|---:|---|
| Display | 官网大标题 | 56px | 66px | 900 | 仅用于 Landing Hero，不用于工作台 |
| Hero Title | 登录页视觉标题 | 36px | 48px | 900 | 大面积引导视觉 |
| H1 / Page Title | 工作台页面主标题 | 34px | 44px | 700 | 如“欢迎回来，钱先生”“评图报告 美术馆方案” |
| H1 Secondary | 登录卡片标题、空状态标题 | 28px | 36/40px | 700 | 用于强提示，但低于 Page Title |
| Feature Title | 等待页主状态标题、预览卡大标题 | 26px | 34px | 700 | 用于单页核心状态 |
| Modal Title | 弹窗标题、详情卡标题 | 22px | 30/32px | 700 | 如“选择新建评图方式”“个人资料” |
| H2 / Section Title | 页面卡片标题 | 18px | 24px | 700 | 如“最近评图项目”“项目信息”“上传图纸” |
| H3 / Subsection | 报告区块、侧栏品牌补充标题 | 16px | 22px | 700 | 如“整体评价”“评分维度” |
| Card Item Title | 项目卡片标题、选项卡标题 | 14px | 20px | 700 | 如项目名称、图纸名称 |
| Body | 正文和说明 | 14px | 22px | 400/500 | 用于页面副标题、较长说明 |
| Body Compact | 卡片说明、表单值 | 13px | 20px | 400/500 | 用于弹窗说明、输入值、历史趋势说明 |
| Label | 表单标签、字段名 | 12px | 16px | 700 | 如“账号 / 邮箱”“图纸类型” |
| Caption | 卡片说明、状态说明 | 12px | 18px | 400/500 | 如项目状态、公告列表、聊天内容 |
| Badge Text | 状态标签 | 11px | 16px | 700 | 如“已完成”“进行中”“草稿” |
| Meta | 日期、账号说明、辅助元信息 | 10-11px | 13-14px | 400/500 | 如“2026/05/31”“Plus” |
| Button Text | 标准按钮 | 14px | 20px | 700 | 主要流程按钮 |
| Button Text Small | 小按钮 | 12px | 16px | 700 | 如项目卡“查看详情”、弹窗底部小按钮 |
| Nav Text | 左侧导航 | 13px | 18px | 500/700 | 选中状态使用 700 |

### 4.3 字体层级规则

- 页面中只能有一个 H1。
- 卡片标题统一使用 H2，不允许同一页面有的卡片 `18px`，有的卡片 `24px`，除非该卡片是当前页面唯一核心状态。
- 正文与说明文字必须通过行高和间距保证可读，不要把正文压到 `10px`。
- `10px` 只用于日期、头像辅助信息、极小 badge，不用于主要说明。
- 可点击文字如果属于按钮，应按 Button Text 处理；如果只是链接，如“详情”“查看更多”，使用 `11-12px / 700 / #6c4dff`。
- 选中状态必须同时使用背景或字重变化，不只改变颜色。

## 5. 颜色系统

### 5.1 中性色

| Token | 色值 | 用途 |
|---|---|---|
| `color-bg-page` | `#f4f6f8` | 页面背景 |
| `color-bg-panel` | `#fafbfc` | 浅色卡片、侧栏、输入底 |
| `color-bg-card` | `#ffffff` | 主卡片、弹窗 |
| `color-text-primary` | `#171719` | 标题、主要文字、深色按钮 |
| `color-text-secondary` | `#53565e` | 正文说明 |
| `color-text-muted` | `#6b7280` / `#6b7385` | 次级说明 |
| `color-text-tertiary` | `#9a9ea7` | 日期、占位、弱提示 |
| `color-border` | `#e8ebef` | 默认边框 |
| `color-divider` | `#eef0f3` | 分割线 |
| `color-hover` | `#eef0f4` | 侧栏和列表 hover |

### 5.2 品牌与状态色

| Token | 色值 | 用途 |
|---|---|---|
| `color-primary` | `#6c4dff` | 主按钮、选中、重点状态 |
| `color-primary-soft` | `#efe9ff` | 紫色弱背景 |
| `color-primary-tint` | `#f1eeff` | 进行中状态背景 |
| `color-success` | `#16a34a` | 已完成、分数通过 |
| `color-success-soft` | `#eaf8ef` | 已完成背景 |
| `color-draft` | `#64748b` | 草稿状态 |
| `color-draft-soft` | `#f1f5f9` | 草稿背景 |
| `color-info` | `#3b82f6` | 上传中、提示信息 |
| `color-info-soft` | `#e6f0ff` | 信息背景 |
| `color-danger` | `#ff5570` / `#dc2626` | 删除、错误、必须修改 |
| `color-danger-soft` | `#ffe8ec` | 危险弱背景 |

使用规则：

- 紫色只用于关键行动、当前选择、进行中、系统回答和可点击链接。
- 绿色只表示完成、通过、分数已生成。
- 灰色只表示草稿、未提交、次要信息。
- 删除和危险操作必须使用红色文字或图标，并与普通操作分组或拉开间距。

## 6. 间距系统

以 `4px` 为基础单位：

| Token | 值 | 用途 |
|---|---:|---|
| `space-1` | 4px | 图标内距、极小间隔 |
| `space-2` | 8px | 标签与文字、小列表间距 |
| `space-3` | 12px | 输入内距、按钮内距 |
| `space-4` | 16px | 卡片内距、小卡片 padding |
| `space-5` | 20px | 选项卡 padding |
| `space-6` | 24px | 卡片默认内距、栅格间距 |
| `space-7` | 28px | 弹窗内距 |
| `space-8` | 32px | 大卡片内距、弹窗标题区 |
| `space-10` | 40px | 顶部按钮高度、流程主按钮 |
| `space-12` | 48px | 侧栏项目行高度 |

规则：

- 工作台卡片之间优先使用 `24px`。
- 弹窗内部左右 padding 优先使用 `28px` 或 `32px`。
- 表单字段上下间距优先使用 `16px` 或 `20px`。
- 左侧栏内部项目列表行高固定 `48px`。

## 7. 圆角与阴影

### 7.1 圆角

| Token | 值 | 用途 |
|---|---:|---|
| `radius-xs` | 8px | 小按钮、小图纸卡 |
| `radius-sm` | 10px | 菜单项、报告行 |
| `radius-md` | 12px | 项目卡、输入框、普通按钮 |
| `radius-lg` | 14px | 导航项、聊天输入 |
| `radius-xl` | 16px | 主按钮、选项卡 |
| `radius-2xl` | 18px | 首页主卡片、阶段卡 |
| `radius-panel` | 20px | 普通白色面板 |
| `radius-modal` | 22px | 中央弹窗 |
| `radius-sidebar` | 24px | 左侧系统栏 |
| `radius-hero` | 26-28px | 官网预览、登录视觉大卡 |

### 7.2 阴影

默认卡片阴影：

```css
box-shadow: 0 1px 2px rgba(17, 19, 24, 0.03),
            0 18px 46px -26px rgba(17, 19, 24, 0.06);
```

侧栏阴影：

```css
box-shadow: 0 20px 24px rgba(23, 23, 26, 0.1);
```

规则：

- 普通卡片、弹窗、菜单使用默认卡片阴影。
- 侧栏使用更明显的侧栏阴影。
- 不叠加多层强阴影，不使用彩色阴影。

## 8. 组件规范

### 8.1 按钮

标准按钮：

- 高度：`40px`
- 圆角：`12px` 或 `16px`
- 字体：`14px / 20px / 700`
- 左右 padding：`20px`

小按钮：

- 高度：`30px`
- 圆角：`8px`
- 字体：`12px / 16px / 700`

按钮类型：

- 主要按钮：黑底 `#171719` 或紫底 `#6c4dff`，白字。
- 次要按钮：白底、`#e8ebef` 边框、深色文字。
- 禁用按钮：透明度 `0.6`，不改变布局尺寸。
- 危险按钮：红色文字，慎用红底。

### 8.2 输入框与文本域

输入框：

- 高度：`40px` 或登录表单 `44px`
- 圆角：`12px`
- 边框：`1px #e8ebef`
- 背景：`#ffffff`
- 字体：`13px / 20px / 400`
- 标签：`12px / 16px / 700`

文本域：

- 圆角：`12px`
- 字体：`13px / 20px`
- placeholder 使用 `#9a9ea7`

### 8.3 卡片

普通白色卡片：

- 背景：`#ffffff`
- 边框：`1px #e8ebef`
- 圆角：`20px`
- 阴影：默认卡片阴影

项目卡片：

- 尺寸：`366 × 140`
- 背景：`#fafbfc`
- 边框：`1px #e8ebef`
- 圆角：`12px`
- 标题：`14px / 20px / 700`
- 元信息：`11px / 16px / 500`
- 状态说明：`12px / 18px / 500`
- 分数：`20px / 24px / 700`
- “查看详情”按钮：`96 × 30`，`12px / 16px / 700`

### 8.4 Badge

标准 badge：

- 高度：`24px` 或 `28px`
- 圆角：`999px`
- 字体：`11px / 16px / 700`
- 水平 padding：`12px`

状态对应：

- 进行中：紫色 `#6c4dff`，背景 `#f1eeff`
- 已完成：绿色 `#16a34a`，背景 `#eaf8ef`
- 草稿：灰色 `#64748b`，背景 `#f1f5f9`

### 8.5 左侧系统栏

系统栏：

- 尺寸：`248 × 796`
- 背景：`#fafbfc`
- 圆角：`24px`
- 边框：`1px #e8ebef`
- 顶部 Logo：`32 × 32`

导航项：

- 尺寸：`204 × 38`
- 圆角：`14px`
- 字体：`13px / 18px`
- 默认字重：`500`
- 选中字重：`700`
- 选中背景：`#e8ebef`
- hover 背景：`#eef0f4`

项目行：

- 尺寸：`204 × 48`
- 圆角：`14px`
- 项目名：`12px / 16px`，选中时 `700`
- 日期：`10px / 13px`
- hover 时显示版本箭头和三点按钮。
- 首页状态下不高亮具体项目；进入项目、报告、等待、确认等项目上下文页后才高亮对应项目。

项目版本：

- 同名项目归并为一个项目。
- 版本按提交时间从早到晚编号为 `V1`、`V2`、`V3`。
- 展开版本时只显示文字和分数，不使用彩色卡片。
- 鼠标进入版本行时背景变为 `#eef0f4`。

菜单：

- 宽度建议：`132px`
- 圆角：`10px`
- 菜单项高度：`32px`
- 菜单文字：`12px / 18px`
- 点击菜单外区域自动收起。

### 8.6 弹窗

普通弹窗：

- 尺寸基准：`584 × 508` 或按 Figma 页面单独设定。
- 圆角：`22px`
- 背景：`#ffffff`
- 遮罩：`rgba(23, 23, 25, 0.3)`
- 标题：`22px / 30px / 700`
- 正文：`14px / 22-28px`
- 底部按钮右对齐。

新建评图方式弹窗：

- 尺寸：`588 × 414`
- 标题：`22px / 30px / 700`
- 说明：`13px / 20px`
- 选项卡：`250 × 210`，圆角 `16px`

## 9. 页面规范

### 9.1 官网首页

- Hero Title 使用 Display：`56px / 66px / 900`
- Hero Body：`16px / 32px / 400`
- 顶部导航：`14px / 20px / 500`
- 产品预览卡不需要完全跟工作台等比例，但颜色、圆角、阴影应保持同一系统。

### 9.2 登录页

- 左侧视觉标题：`36px / 48px / 900`
- 登录卡标题：`28px / 40px / 700`
- 表单标签：`12px / 16px / 700`
- 表单值：`13px / 20px / 400`
- 密码点可使用 `16px` 并加字距。

### 9.3 项目首页

- 页面标题：`34px / 44px / 700`
- 页面副标题：`14px / 22px`
- 主卡片标题：`18px / 24px / 700`
- 右侧公告/新手引导标题与主卡片标题保持同级，不再使用过大的标题。
- 空状态标题：`28px / 36px / 700`
- 空状态说明：`14px / 20px`

### 9.4 新建流程页

- 页面标题沿用 H1。
- 表单卡片标题使用 H2：`22px / 30px / 700` 可作为流程页主表单标题。
- 表单标签统一 `12px / 16px / 700`。
- 表单内容统一 `13px / 20px / 400`。
- 阶段卡和 Agent 卡标题使用 `18px / 24px / 700`。

### 9.5 等待评图页

- 主状态标题：`26px / 34px / 700`
- 进度卡标题：`22px / 29px / 700`
- 指标数字：`20px / 26px / 700`
- 进度说明、聊天气泡：`12px / 18-19px`

### 9.6 报告页

- 报告页标题：`34px / 44px / 700`
- 报告页版本号：`24px / 32px / 700`，颜色使用次级灰。
- 总分数字：`34px / 36px / 700`
- 维度大卡标题：`22px / 28px / 700`
- 维度分数：`48px / 54px / 700`
- 报告正文：`12px / 20px`
- 聊天气泡：`12px / 20px`
- 追问快捷按钮：`11px / 16px / 700`

## 10. 文案与信息层级

文案层级：

- 页面标题回答“这是哪个页面”。
- 页面副标题回答“用户可以在这里做什么”。
- 卡片标题回答“这个模块是什么”。
- 卡片正文回答“当前状态或操作说明”。
- 按钮文字回答“点击后发生什么”。

文案规则：

- 按钮使用动词短语，如“确认提交”“查看详情”“下载报告”。
- 状态标签使用短词，如“已完成”“进行中”“草稿”。
- 不新增与 Figma 或业务流程无关的装饰性文案。
- 同一操作在不同页面保持相同名称，例如“查看详情”不要混用“查看”“详情”“打开详情”。

## 11. 交互状态

通用状态：

- Hover：浅灰背景 `#eef0f4` 或边框保持不变。
- Active / Selected：浅灰背景 `#e8ebef` + 字重加粗，必要时图标变紫。
- Focus：保留浏览器可访问性，但视觉上不增加强烈外圈。
- Disabled：透明度 `0.6`，鼠标不可点击。
- Loading：优先保留原卡片结构，不让内容区域先变空。

侧栏状态：

- 首页不显示项目选中态。
- 点击项目进入最新版本。
- 点击具体版本进入对应版本报告或状态页。
- 三点菜单打开后，点击其他区域自动关闭。

## 12. 实现约束

React + Tailwind 实现时应遵守：

- 页面固定使用 `1536 × 820` 设计画布，不在单个页面重新定义画布尺寸。
- 新增组件时先确认是否能复用 `Button`、`Card`、`Sidebar`、`Badge`、`PageTitle`。
- 不随意新增硬编码颜色；优先使用本文档中的 token。
- 不随意新增字号；必须映射到 Typography Scale。
- 同一层级文字跨页面必须一致，例如所有工作台 Page Title 都用 `34px / 44px / 700`。
- 如果 Figma 像素级还原与本规范冲突，短期以 Figma 为准，但应记录差异并在下一轮统一规范。

## 13. 推荐 Tailwind 映射

常用映射：

```txt
Page Title: text-[34px] leading-[44px] font-bold
Modal Title: text-[22px] leading-[30px] font-bold
Card Title: text-[18px] leading-6 font-bold
Subsection: text-[16px] leading-[22px] font-bold
Body: text-[14px] leading-[22px]
Body Compact: text-[13px] leading-5
Caption: text-[12px] leading-[18px]
Meta: text-[10px] leading-[13px]
Button: text-[14px] leading-5 font-bold
Small Button: text-[12px] leading-4 font-bold
Nav: text-[13px] leading-[18px]
```

常用颜色：

```txt
bg-page: bg-[#f4f6f8]
bg-panel: bg-[#fafbfc]
bg-card: bg-white
text-primary: text-[#171719]
text-secondary: text-[#53565e]
text-muted: text-[#6b7280]
text-tertiary: text-[#9a9ea7]
border-default: border-[#e8ebef]
primary: #6c4dff
primary-soft: #efe9ff
hover: #eef0f4
selected: #e8ebef
```

## 14. 后续维护规则

- 每次新增页面或组件前，先检查本规范是否已有对应层级和组件样式。
- 如果发现当前前端与本规范不一致，应优先统一到本规范，除非用户明确要求继续保持 Figma 中的特殊样式。
- 如果 Figma 后续新增正式 Design Tokens，应把本文档中的颜色、字号、间距迁移为代码中的统一 token。
